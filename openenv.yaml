name: "Cloud Cost Optimizer"
description: "Agent manages server infrastructure to optimize costs safely."
version: "1.0.0"

# This is what the grading robot was looking for!
server:
  port: 7860

tasks:
  - name: "easy_cost_cut"
    difficulty: "easy"
